---
# yaml-language-server: $schema=https://schema.blue-bui>type: script
modules:
  - type: containerfile
    snippets:
      - COPY --from=ghcr.io/ublue-os/main-kernel:41 /tmp/rpms /tmp/rpms/kernel
  - type: script
    scripts:
      - fixupstreamicdloaderbug.sh
      - installsignedkernel.sh
